apiVersion: v1
kind: Service
metadata:
  name: {{ include "fcsapp.fullname" . }}
  labels:
    {{- include "fcsapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fcsapp.selectorLabels" . | nindent 4 }}
