apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fcsapp.fullname" . }}-test-connection"
  labels:
    {{- include "fcsapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fcsapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
