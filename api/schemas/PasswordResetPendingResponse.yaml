type: object
required:
  - resend_after
  - expires_at
properties:
  resend_after:
    type: string
    format: date-time
    description: Earliest time the client may call /api/password-reset/resend-code
  expires_at:
    type: string
    format: date-time
    description: When the reset code becomes invalid
