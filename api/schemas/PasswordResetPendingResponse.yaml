type: object
required:
  - reset_token
  - resend_after
  - expires_at
properties:
  reset_token:
    type: string
    format: uuid
  resend_after:
    type: string
    format: date-time
  expires_at:
    type: string
    format: date-time
