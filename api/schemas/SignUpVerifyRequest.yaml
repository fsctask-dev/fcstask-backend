type: object
required:
  - verification_token
  - code
properties:
  verification_token:
    type: string
    format: uuid
  code:
    type: string
    description: 6-digit verification code from the email
