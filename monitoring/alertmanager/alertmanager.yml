global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "severity"]
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: "fcstask-monitor-bot"

receivers:
  - name: "fcstask-monitor-bot"
    webhook_configs:
      - url: "http://132.243.119.165:8080/webhook/alerts"
